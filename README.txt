This is maven project.

1) in order to build project type "mvn package"
2) in order to build poroject and run integration(Selenium) test type "mvn verify"(it will start tomcate 7 instance and run all integraion test, after that shutdown tomcat instance)