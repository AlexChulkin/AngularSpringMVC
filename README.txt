This is maven project.

1) in order to build project type "mvn package"
2) in order to build project and run integration(Selenium) tests type "mvn verify"(it will start tomcat 7 instance and run all integration tests, after that shutdown tomcat instance)